- In all interactions and commit messages, be extremely concise and sacrifice grammar fro the sake of concision.

## Editor

- Use `nvim`

## Bash commands

- Use `rg` instead of `grep`
- Use `fd` instead of `find`

## Plans

- At the end of each plan, give me a list of unresolved questions to anwser, if any. Make the question extremely concise. Sacrifice grammar for the sake of concision.

